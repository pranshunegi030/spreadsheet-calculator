<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Edit Opex</title>
    <link rel="stylesheet" href="/styles.css" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      body {
        background-color: #f7f9fc;
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 20px;
      }
      h1 {
        margin-bottom: 20px;
        font-size: 24px;
        color: #333;
        text-align: center;
      }
      form {
        background-color: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        margin-bottom: 20px;
        width: 100%;
        max-width: 900px;
      }
      form input,
      form button {
        flex: 1 1 45%;
        padding: 10px;
        font-size: 16px;
        border: 1px solid #ccc;
        border-radius: 4px;
        margin-bottom: 10px;
      }
      form button {
        background-color: #4caf50;
        color: white;
        cursor: pointer;
        transition: background-color 0.3s;
      }
      form button:hover {
        background-color: #45a049;
      }
      @media (max-width: 600px) {
        form input,
        form button {
          flex: 1 1 100%;
        }
      }
      @media (max-width: 400px) {
        h1 {
          font-size: 20px;
        }
        form {
          padding: 15px;
        }
        form input,
        form button {
          padding: 8px;
          font-size: 14px;
        }
      }
    </style>
  </head>
  <body>
    <h1>Edit Opex for Company: <%= companyName %>, Case: <%= caseName %></h1>
    <form action="/editOpexDisc/<%= opex.id %>" method="POST">
      <input type="hidden" name="companyId" value="<%= opex.company_id %>" />
      <input type="hidden" name="caseId" value="<%= opex.case_id %>" />
      <input type="text" name="item" value="<%= opex.item %>" required />
      <input
        type="number"
        name="rate"
        value="<%= opex.rate %>"
        min="0"
        step="0.01"
        required
      />
      <input
        type="number"
        name="quantity"
        value="<%= opex.quantity %>"
        min="0"
        required
      />
      <input
        type="number"
        name="payment_year"
        value="<%= opex.payment_year %>"
        min="0"
        required
      />
      <button type="submit">Update Opex</button>
    </form>
  </body>
</html>
