<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Edit Opex</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <h1>
      Edit Opex for Company ID: <%= companyId %> and Case ID: <%= caseId %>
    </h1>
    <nav>
      <a href="/">Home</a>
      <a href="/logout">Logout</a>
      <div class="dropdown">
        <button class="dropbtn">Account</button>
        <div class="dropdown-content">
          <a href="#">User: <%= user.username %></a>
        </div>
      </div>
    </nav>
    <form
      action="/editOpexDisc/<%= opex.id %>?companyId=<%= companyId %>&caseId=<%= caseId %>"
      method="POST"
    >
      <input type="hidden" name="companyId" value="<%= companyId %>" />
      <input type="hidden" name="caseId" value="<%= caseId %>" />
      <input type="text" name="item" value="<%= opex.item %>" required />
      <input
        type="number"
        name="rate"
        value="<%= opex.rate %>"
        min="0"
        step="0.01"
        required
      />
      <input
        type="number"
        name="quantity"
        value="<%= opex.quantity %>"
        min="0"
        required
      />
      <input
        type="number"
        name="payment_year"
        value="<%= opex.payment_year %>"
        min="0"
        required
      />
      <button type="submit">Update Opex</button>
    </form>
  </body>
</html>
