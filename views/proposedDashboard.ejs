<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Proposed Solution Dashboard</title>
    <link rel="stylesheet" href="/styles.css" />
    <style>
      body {
        background-color: #f7f9fc;
        font-family: Arial, sans-serif;
      }
      .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
        text-align: center;
      }
      h1 {
        margin-bottom: 20px;
        font-size: 24px;
        color: #333;
      }
      .button-row {
        display: flex;
        justify-content: space-between;
        margin-bottom: 20px;
      }
      .button-card {
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        padding: 20px;
        transition: transform 0.2s;
        flex: 1;
        margin: 0 10px;
      }
      .button-card a {
        text-decoration: none;
        color: #007bff;
        font-weight: bold;
        display: block;
      }
      .button-card:hover {
        transform: translateY(-5px);
      }

      /* Responsive Styles */
      @media (max-width: 768px) {
        h1 {
          font-size: 20px;
        }
        .button-card {
          padding: 15px;
          margin: 10px 0;
        }
        .button-card a {
          font-size: 16px;
        }
        .button-row {
          flex-direction: column;
        }
      }

      @media (max-width: 480px) {
        h1 {
          font-size: 18px;
        }
        .button-card {
          padding: 10px;
          margin: 8px 0;
        }
        .button-card a {
          font-size: 14px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>
        Proposed Solution Dashboard for <%= companyName %>, <%= caseName %>
      </h1>
      <div class="button-row">
        <div class="button-card">
          <a href="/addCapex/<%= companyId %>/<%= caseId %>">Add Capex</a>
        </div>
        <div class="button-card">
          <a href="/addOpex/<%= companyId %>/<%= caseId %>">Add Opex</a>
        </div>
        <div class="button-card">
          <a href="/viewSummary/<%= companyId %>/<%= caseId %>">View Summary</a>
        </div>
      </div>
    </div>
  </body>
</html>
